// src/config.js

// ⚠️ Update this IP whenever it changes (check ipconfig for current IPv4)
const LOCAL_IP = '192.168.254.122';
const PORT = '5207';

export const API_BASE_URL = `http://${LOCAL_IP}:${PORT}/api`;